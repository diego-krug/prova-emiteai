spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/prova
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  mvc:
    static-path-pattern: /**
  web:
    resources:
      static-locations: classpath:/static/

  rabbitmq:
    addresses: amqps://wiotfugq:r8SA1v3WIS61yvI199Qc5eAuxqNtopis@jackal.rmq.cloudamqp.com/wiotfugq
    username: wiotfugq
    password: r8SA1v3WIS61yvI199Qc5eAuxqNtopis
    virtual-host: wiotfugq
    queues:
      relatorio: emiteai_relatorio
      cadastro: emiteai_cadastro
    listener:
      simple:
        concurrency: 5
        max-concurrency: 10

server:
  port: 8080

logging:
  level:
    root: INFO
    org.springframework.web: DEBUG